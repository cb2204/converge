# docs/archive — superseded blueprints

These are kept for provenance. **They are not current.** Each was accurate when
written; each is now wrong in ways that matter.

| Archived | Superseded by | Why it was retired |
|---|---|---|
| `cvg-aut-systems-spine-steps-v5.pdf` | [`../converge-method-v6.pdf`](../converge-method-v6.pdf) | Numbered `Bind` as Pass 6 and the Manager as Pass 7; documented a `stack-to-harness` "Pass 6 Harness" skill that has since been **deleted**; counted **13 skills** (now 12); and did not foreground the barrier at Consensus as the organising idea. |
| `task-spec-v3.2.0.pdf` | [`../task-spec-v3.6.0.pdf`](../task-spec-v3.6.0.pdf) | Predates the six-tier engine: it still documents the `XL → SDD` escape hatch (removed in 3.4), the four-tier effort model, and the `glm`-only L tier (removed in 3.6). |

The current documents are generated from HTML sources in
[`../src/`](../src/) — see that directory's `render.sh`.

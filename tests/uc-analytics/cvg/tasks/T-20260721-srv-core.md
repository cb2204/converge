---
id: T-20260721-srv-core
title: "Serve — read-only gold query core (below-seam isolation)"
status: ready
format_version: 3
profile: lite
effort: S
budget_iterations: 8
agent: any
depends_on: [T-20260721-tf-publish]
touches_paths: []
creates_paths:
  - cvg/serve/core/reader.py
  - cvg/serve/tests/test_isolation.py
source_note: "R5.R — Pass 5B tasking of the backbone (Fork B, task-driven)"
created: 2026-07-21T00:00:00Z
execution_backend: any
signed_off: true
signed_off_by: luanmorenomaciel
signed_off_at: 2026-07-25T08:17:47Z
tracker_ref: linear:CVG-27
projection:
  milestone: Serve
signed_off_sig: hmac-sha256-v2:1f197c76:17cd0f2f76e921bac3cef907235673d47fc9f3d91fc3ceff184b552369d2ef7a
---

# Serve — read-only gold query core (below-seam isolation)

## Goal
One read-only reader per published gold table, contract-only — touches `gold.*` and never silver / `raw.*` / the source (ADR-0001). A request for a field not in `gold.*` is refused, not resolved by a deeper read.

## Success Criteria
```bash
# Every check asserts DATABASE STATE or EXECUTES the artifact — never file
# shape. A stub file cannot create a table, populate a column, or revoke a
# privilege. `make up` must be running (uc-analytics-postgres).
PG="docker exec uc-analytics-postgres psql -U postgres -d ecommerce -tAc"

# B-1: the reader RUNS and returns a real answer out of gold.
eval_1() {
  test -f cvg/serve/core/reader.py || return 1
  out=$(python3 cvg/serve/core/reader.py 2>/dev/null)
  printf '%s' "$out" | grep -qE '[0-9]'
}

# B-2: isolation is enforced by GRANTS, not by discipline. The serving role
# must be unable to read below the seam even if the code tried.
eval_2() {
  test -f cvg/serve/tests/test_isolation.py || return 1
  python3 cvg/serve/tests/test_isolation.py >/dev/null 2>&1 || return 1
  role=$($PG "select rolname from pg_roles where rolname like '%serve%' and rolname <> 'postgres' limit 1" 2>/dev/null)
  [ -n "$role" ] || return 1
  raw_ok=$($PG "select has_schema_privilege('$role','raw','USAGE')" 2>/dev/null)
  [ "$raw_ok" = "f" ]
}
```

## Validation Card
```yaml
success_criteria:
  - {id: eval_1, description: core reads gold only, never below the seam, runnable: bash, check_type: deterministic, terminal: false, expected_duration_sec: 3}
  - {id: eval_2, description: below-seam isolation is tested, runnable: bash, check_type: deterministic, terminal: true, expected_duration_sec: 3}
retry_policy:
  max_iterations: 8
  circuit_breaker_no_progress: 3
  on_terminal_failure: park_with_context
agent_contract:
  version: 2
  read: [intent, contract]
  produce: [code, test]
  required_tools: [bash]
  timeout_minutes: 30
  sandbox_type: host
  output_artifacts: []
  mcp_dependencies: []
  emit: [pass, fail]
  backend_metadata: {}
```

## Exit Check
```bash
eval_1 && eval_2
```

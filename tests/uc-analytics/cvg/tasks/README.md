# tasks/ — sealed execution units (reserved)

Born at Pass 5B. EXCEPTION today: the task-spec tooling anchors tasks/ at
git root, so specs live in the converge repo-root tasks/ until this
project becomes its own repository — then they move here. This folder is
reserved so the workspace shape is already true.

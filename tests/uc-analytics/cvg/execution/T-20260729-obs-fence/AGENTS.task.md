# Task brief — T-20260729-obs-fence

> Generated by Pass 7 · Bind. **The signed Task-Spec is the only instruction source** — this brief points at it, never replaces it. It holds identifiers, not content: open what you need, when you need it.

- **Spec (canonical):** `cvg/tasks/T-20260729-obs-fence.md`
- **Contract:** `cvg/execution/T-20260729-obs-fence/execution-profile.yaml`
- **Epoch:** `T-20260729-obs-fence@08a672ad2dae`
- **Project conventions:** `CLAUDE.md` (read it once; it is the router)

## Goal

A stdlib-only script `cvg/capture/schema_fence.py` that statically scans the ANALYTICAL capture surfaces — `cvg/capture/pipelines/` and `cvg/capture/dagster/` — for any reference to the `_control` schema. Clean scan: print `FENCE=OK`, exit 0.

## You may write ONLY these paths

- `cvg/capture/schema_fence.py`

**Never touch:**

- `cvg/capture/pipelines/orders.py`
- `cvg/capture/principal.sql`
- `cvg/capture/tests/ (ground-truth probes)`
- `src/ (the product's schema, seed, and generator)`

## Done means

Done is not your judgement — it is this command exiting zero from a clean checkout:

```bash
```bash
# Final proof-of-done. Returns 0 only when ALL evals pass.
eval_1 && eval_2 && eval_3
```

---
```

## Boundaries

- network: **deny** · external writes: **deny**
- enforcement on `generic`: **detect** (`prevent` = blocked before it happens; `detect` = caught at settlement)
- authority closes on: settle, block, budget_exhausted, epoch_change — a new epoch requires a fresh bind

## Before you settle

```bash
python3 skills/task-to-runtime-contract/scripts/check-path-policy.py --profile cvg/execution/T-20260729-obs-fence/execution-profile.yaml --base "$CVG_BASE_REF"
```

If you need something outside this scope, **stop and request a re-bind**. Widening scope yourself invalidates the seal and the work will be rejected.

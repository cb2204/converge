# Plan · reporting layer

lane-meta: thread=no · risk=<low|med|high> · owner=<stream>

Component **C · Reporting** from the decomposition. Input contract: `order.*`.

Plan altitude only — this fixture is altitude-clean; it fails ONLY the seam
economics: the set has no steel-thread lane, risk/owner are unfilled, and this
downstream lane states no seam-evolution rule.

## Features / components

- **daily-rollup** — aggregate orders per day (responsibility, not the query).

## The interface this lane consumes (the seam — hard boundary)

| upstream unit | fields consumed | read by (this lane's piece) |
|---|---|---|
| `order` | order_id | daily-rollup |

## Dependencies

```
order  ->  daily-rollup
```

- The single inbound seam is `order.*`.

## Build order

1. daily-rollup.

## Tests that prove each piece

| Piece | Tests (what they prove) |
|---|---|
| **daily-rollup** | counts each order once per day. Proves: no double count. |

## Open questions

| # | Item | Owner | Blocks build? |
|---|---|---|---|
| Q1 | reporting timezone? | data | No |

## Spec traceability

- **ADR-0009** — reads the published order contract.
